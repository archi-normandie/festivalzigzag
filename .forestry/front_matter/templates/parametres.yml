---
label: Paramètres
hide_body: true
fields:
- name: footer
  type: field_group
  config: {}
  fields:
  - name: blocs
    type: blocks
    label: Blocs
    template_types:
    - bloc-content
    - bloc-navigation
    config:
      min: 
      max: 3
  label: Pied de page
- name: events-categories
  type: field_group_list
  fields:
  - name: category
    type: text
    config:
      required: false
    label: Catégorie
  config:
    min: 
    max: 
    labelField: category
  label: Catégories d'évenements
pages:
- static/configuration/settings.json
