---
label: Paramètres
hide_body: true
fields:
- name: footer
  type: field_group
  config: {}
  fields:
  - name: blocks
    type: field_group_list
    config:
      min: 
      max: 
      labelField: 
    label: Blocs
  - name: navigation
    type: field_group_list
    fields:
    - name: label
      type: text
      config:
        required: true
      label: Nom du groupe
    - name: links
      type: field_group_list
      fields:
      - name: name
        type: text
        config:
          required: true
        label: Nom
      - name: path
        type: text
        config:
          required: false
        label: Chemin
      config:
        min: 
        max: 
        labelField: 
      label: Liens
    config:
      min: 
      max: 
      labelField: 
    label: Navigation
  label: Pied de page
pages:
- static/configuration/settings.json
