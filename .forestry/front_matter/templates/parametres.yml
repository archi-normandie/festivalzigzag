---
label: Paramètres
hide_body: true
fields:
- name: add_left_content
  type: boolean
  label: Ajouter un contenu à gauche
- name: left_content
  type: field_group
  config: {}
  fields:
  - name: contenu
    type: textarea
    default: ''
    config:
      required: false
      wysiwyg: true
      schema:
        format: markdown
    label: Contenu
  label: Contenu gauche
  showOnly:
    field: add_left_content
- name: footer
  type: field_group
  config: {}
  fields:
  - name: navigation
    type: field_group_list
    fields:
    - name: label
      type: text
      config:
        required: true
      label: Nom du groupe
    - name: links
      type: field_group_list
      fields:
      - name: name
        type: text
        config:
          required: true
        label: Nom
      - name: path
        type: text
        config:
          required: false
        label: Chemin
      config:
        min: 
        max: 
        labelField: 
      label: Liens
    config:
      min: 
      max: 
      labelField: 
    label: Navigation
  label: Pied de page
pages:
- static/configuration/settings.json
