---
label: Paramètres
hide_body: true
fields:
- name: footer
  type: field_group
  config: {}
  fields:
  - name: blocs
    type: blocks
    label: Blocs
    template_types:
    - bloc-content
    - bloc-navigation
    config:
      min: 
      max: 3
  label: Pied de page
- name: events
  type: field_group
  config: {}
  fields:
  - name: categories
    type: field_group_list
    fields:
    - name: category
      type: text
      config:
        required: false
      label: Catégorie
    config:
      min: 
      max: 
      labelField: 
    label: Catégories
  label: Évémenents
pages:
- static/configuration/settings.json
